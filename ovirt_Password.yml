# You need to fill in the secrets in this file and encrypt it with ansible-vault

ovirt_password: "<rhv password>"
rhn_password: "<password for rhn>"
apikey: "<api key for ClearOS to get leases from DHCP rest API>"
pull_secret: '<pull secret from openshift account for pulling quay containers during bootstrap>'
ssh_key: '<ssh public key for access to coreos and the rhel templates>'
